/// <reference types="node" />
import net from 'net';
import { Logger } from '../../logger';
import { ApiNamespace } from '../routes';
import { RpcSocketAdapter } from './socketAdapter/socketAdapter';
export declare class RpcIpcAdapter extends RpcSocketAdapter {
    constructor(path: string, logger: Logger | undefined, namespaces: ApiNamespace[]);
    protected createServer(): net.Server;
}
//# sourceMappingURL=ipcAdapter.d.ts.map