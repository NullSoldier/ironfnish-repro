/// <reference types="node" />
import net from 'net';
import { FileSystem } from '../../fileSystems';
import { Logger } from '../../logger';
import { IronfishNode } from '../../utils';
import { ApiNamespace } from '../routes';
import { RpcSocketAdapter } from './socketAdapter/socketAdapter';
export declare class RpcTlsAdapter extends RpcSocketAdapter {
    readonly fileSystem: FileSystem;
    readonly nodeKeyPath: string;
    readonly nodeCertPath: string;
    node: IronfishNode;
    constructor(host: string, port: number, fileSystem: FileSystem, nodeKeyPath: string, nodeCertPath: string, node: IronfishNode, logger: Logger | undefined, namespaces: ApiNamespace[]);
    protected createServer(): Promise<net.Server>;
}
//# sourceMappingURL=tlsAdapter.d.ts.map