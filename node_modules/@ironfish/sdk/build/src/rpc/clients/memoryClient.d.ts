import { Logger } from '../../logger';
import { MemoryResponse } from '../adapters';
import { Router } from '../routes';
import { RpcClient } from './client';
export declare class RpcMemoryClient extends RpcClient {
    router?: Router;
    constructor(logger: Logger, router?: Router);
    request<TEnd = unknown, TStream = unknown>(route: string, data?: unknown, options?: {
        timeoutMs?: number | null;
    }): MemoryResponse<TEnd, TStream>;
}
//# sourceMappingURL=memoryClient.d.ts.map