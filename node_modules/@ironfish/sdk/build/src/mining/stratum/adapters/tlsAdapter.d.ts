/// <reference types="node" />
import net from 'net';
import tls from 'tls';
import { Logger } from '../../../logger';
import { StratumTcpAdapter } from './tcpAdapter';
export declare class StratumTlsAdapter extends StratumTcpAdapter {
    readonly tlsOptions: tls.TlsOptions;
    constructor(options: {
        logger: Logger;
        host: string;
        port: number;
        tlsOptions: tls.TlsOptions;
    });
    protected createServer(): net.Server;
}
//# sourceMappingURL=tlsAdapter.d.ts.map