import { Logger } from '../../logger';
import { IDatabase, IDatabaseTransaction } from '../../storage';
import { IronfishNode } from '../../utils';
import { Database, Migration } from '../migration';
export declare class Migration020 extends Migration {
    path: string;
    database: Database;
    prepare(node: IronfishNode): IDatabase;
    forward(node: IronfishNode, _db: IDatabase, tx: IDatabaseTransaction | undefined, logger: Logger): Promise<void>;
    backward(): Promise<void>;
}
//# sourceMappingURL=020-backfill-null-asset-supplies.d.ts.map