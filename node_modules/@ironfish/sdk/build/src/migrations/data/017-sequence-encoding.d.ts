import { Logger } from '../../logger';
import { DatabaseSchema, IDatabase, IDatabaseStore, IDatabaseTransaction } from '../../storage';
import { IronfishNode } from '../../utils';
import { Database, Migration } from '../migration';
export declare class Migration017 extends Migration {
    path: string;
    database: Database;
    prepare(node: IronfishNode): IDatabase;
    forward(node: IronfishNode, db: IDatabase, tx: IDatabaseTransaction | undefined, logger: Logger): Promise<void>;
    backward(node: IronfishNode, db: IDatabase): Promise<void>;
    getOldStores(db: IDatabase): {
        sequenceToNoteHash: IDatabaseStore<DatabaseSchema>;
        sequenceToTransactionHash: IDatabaseStore<DatabaseSchema>;
        pendingTransactionHashes: IDatabaseStore<DatabaseSchema>;
    };
}
//# sourceMappingURL=017-sequence-encoding.d.ts.map