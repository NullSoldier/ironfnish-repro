import { Logger } from '../../logger';
import { IDatabase, IDatabaseTransaction } from '../../storage';
import { IronfishNode } from '../../utils';
import { Database, Migration } from '../migration';
export declare class Migration000 extends Migration {
    path: string;
    database: Database;
    prepare(node: IronfishNode): IDatabase;
    forward(node: IronfishNode, db: IDatabase, tx: IDatabaseTransaction | undefined, logger: Logger): Promise<void>;
    /**
     * Writing a backwards migration is optional but suggested
     */
    backward(node: IronfishNode, db: IDatabase, tx: IDatabaseTransaction | undefined, logger: Logger): Promise<void>;
}
//# sourceMappingURL=000-template.d.ts.map