import { Logger } from '../../logger';
import { IDatabase, IDatabaseTransaction } from '../../storage';
import { IronfishNode } from '../../utils';
import { Database, Migration } from '../migration';
export declare class Migration026 extends Migration {
    path: string;
    database: Database;
    prepare(node: IronfishNode): IDatabase;
    forward(node: IronfishNode, db: IDatabase, _tx: IDatabaseTransaction | undefined, logger: Logger): Promise<void>;
    backward(node: IronfishNode, db: IDatabase): Promise<void>;
}
//# sourceMappingURL=026-timestamp-to-transactions.d.ts.map