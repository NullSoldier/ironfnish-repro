import { Logger } from '../../logger';
import { IDatabase, IDatabaseTransaction } from '../../storage';
import { IronfishNode } from '../../utils';
import { Database, Migration } from '../migration';
export declare class Migration023 extends Migration {
    path: string;
    database: Database;
    prepare(node: IronfishNode): IDatabase;
    forward(_node: IronfishNode, db: IDatabase, tx: IDatabaseTransaction | undefined, logger: Logger): Promise<void>;
    backward(_node: IronfishNode, _db: IDatabase, _tx: IDatabaseTransaction | undefined, _logger: Logger): Promise<void>;
}
//# sourceMappingURL=023-wallet-optional-spending-key.d.ts.map