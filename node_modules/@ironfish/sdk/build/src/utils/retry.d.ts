export declare type RetryStrategy = {
    delay: number;
    jitter: number;
    maxDelay: number;
    maxRetries?: number;
};
export declare class Retry {
    private readonly strategy;
    private attempt;
    constructor(strategy: RetryStrategy);
    try<T>(fn: () => Promise<T>): Promise<T>;
    private tryExecutor;
    private reset;
    private shouldRetry;
    private nextDelay;
}
//# sourceMappingURL=retry.d.ts.map