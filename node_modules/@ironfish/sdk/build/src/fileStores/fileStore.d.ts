import { FileSystem } from '../fileSystems';
import { PartialRecursive } from '../utils';
export declare class FileStore<T extends Record<string, unknown>> {
    files: FileSystem;
    dataDir: string;
    configPath: string;
    configName: string;
    constructor(files: FileSystem, configName: string, dataDir: string);
    load(): Promise<PartialRecursive<T> | null>;
    save(data: PartialRecursive<T>): Promise<void>;
}
//# sourceMappingURL=fileStore.d.ts.map