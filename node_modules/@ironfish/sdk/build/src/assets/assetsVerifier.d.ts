/// <reference types="node" />
import { VerifiedAssetsCacheStore } from '../fileStores/verifiedAssets';
import { Logger } from '../logger';
export declare type AssetVerification = {
    status: 'verified' | 'unverified' | 'unknown';
};
export declare class AssetsVerifier {
    private readonly REFRESH_INTERVAL;
    private readonly logger;
    private readonly api;
    private readonly cache?;
    private readonly retry;
    private started;
    private refreshToken?;
    private verifiedAssets?;
    constructor(options?: {
        apiUrl?: string;
        cache?: VerifiedAssetsCacheStore;
        logger?: Logger;
    });
    start(): void;
    stop(): void;
    private refreshLoop;
    private refresh;
    private saveCache;
    verify(assetId: Buffer | string): AssetVerification;
}
//# sourceMappingURL=assetsVerifier.d.ts.map