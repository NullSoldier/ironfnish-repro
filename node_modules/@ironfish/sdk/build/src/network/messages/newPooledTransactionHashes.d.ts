/// <reference types="node" />
import bufio from 'bufio';
import { TransactionHash } from '../../primitives/transaction';
import { NetworkMessage } from './networkMessage';
export declare class NewPooledTransactionHashes extends NetworkMessage {
    hashes: TransactionHash[];
    constructor(hashes: TransactionHash[]);
    serializePayload(bw: bufio.StaticWriter | bufio.BufferWriter): void;
    static deserializePayload(buffer: Buffer): NewPooledTransactionHashes;
    getSize(): number;
}
//# sourceMappingURL=newPooledTransactionHashes.d.ts.map