import { Blockchain } from '../../blockchain';
import { PrivateIdentity } from '../identity';
import { LocalPeer } from '../peers/localPeer';
/**
 * Utility to create a fake "keypair" for testing the network layer
 */
export declare function mockLocalPeer({ identity, agent, version, chain, }?: {
    identity?: PrivateIdentity;
    agent?: string;
    version?: number;
    chain?: Blockchain;
}): LocalPeer;
//# sourceMappingURL=mockLocalPeer.d.ts.map