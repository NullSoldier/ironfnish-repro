/// <reference types="node" />
import { BurnDescription } from '../../primitives/burnDescription';
import { MintData, RawTransaction } from '../../primitives/rawTransaction';
import { Transaction } from '../../primitives/transaction';
import { Account, Wallet } from '../../wallet';
export declare function isTransactionMine(transaction: Transaction, account: Account): boolean;
export declare function createRawTransaction(options: {
    wallet: Wallet;
    from: Account;
    to?: Account;
    fee?: bigint;
    amount?: bigint;
    expiration?: number;
    assetId?: Buffer;
    outputs?: {
        publicAddress: string;
        amount: bigint;
        memo: string;
        assetId: Buffer;
    }[];
    mints?: MintData[];
    burns?: BurnDescription[];
}): Promise<RawTransaction>;
//# sourceMappingURL=transaction.d.ts.map