import { MerkleTree } from '../../merkletree';
import { IDatabase } from '../../storage';
export declare function makeTree({ name, db, location, depth, leaves, }?: {
    name?: string;
    db?: IDatabase;
    location?: string;
    depth?: number;
    leaves?: string;
}): Promise<MerkleTree<string, string, string, string>>;
//# sourceMappingURL=merkletree.d.ts.map