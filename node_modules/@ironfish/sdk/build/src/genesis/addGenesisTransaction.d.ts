import { Logger } from '../logger';
import { FullNode } from '../node';
import { Block } from '../primitives';
import { Account } from '../wallet';
import { GenesisBlockAllocation } from './makeGenesisBlock';
export declare function addGenesisTransaction(node: FullNode, account: Account, allocations: GenesisBlockAllocation[], logger: Logger): Promise<{
    block: Block;
}>;
//# sourceMappingURL=addGenesisTransaction.d.ts.map